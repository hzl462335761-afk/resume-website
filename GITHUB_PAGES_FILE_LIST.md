# GitHub Pages 上传文件列表

## 📋 完整文件清单

### 方式一：使用 GitHub Actions 自动部署（推荐）

**只需上传源代码文件，GitHub Actions 会自动构建和部署。**

#### 必须上传的文件：

```
项目根目录/
├── .github/
│   └── workflows/
│       └── deploy.yml          # GitHub Actions 部署配置
├── public/
│   ├── .nojekyll                # 禁用 Jekyll 处理
│   ├── robots.txt               # SEO 爬虫配置
│   ├── assets/
│   │   └── resume.pdf           # 简历 PDF
│   └── images/
│       ├── icons/
│       │   ├── wechat.svg
│       │   └── douyin.svg
│       ├── location/
│       │   └── map.svg
│       ├── avatar-guaiyi.svg
│       ├── avatar-leon.svg
│       ├── avatar-new.png
│       ├── og-cover.svg
│       └── touxiang.png
├── src/
│   ├── app/
│   │   ├── layout.tsx           # 包含增强的 SEO metadata
│   │   ├── sitemap.ts           # 自动生成 sitemap.xml
│   │   ├── page.tsx
│   │   ├── globals.css
│   │   └── [其他页面文件]
│   ├── components/
│   ├── data/
│   ├── hooks/
│   ├── modules/
│   ├── styles/
│   └── types/
├── .gitignore
├── DEPLOYMENT.md                # 部署指南
├── GITHUB_PAGES_FILE_LIST.md   # 本文件
├── next.config.ts
├── package.json
├── package-lock.json
├── postcss.config.mjs
├── README.md
├── tailwind.config.ts
└── tsconfig.json
```

### 方式二：手动上传构建后的文件

**如果选择手动部署，需要上传 `out` 文件夹中的所有内容。**

#### 从 `out` 文件夹上传：

```
out/
├── .nojekyll                    # 必须：禁用 Jekyll
├── robots.txt                   # SEO 配置
├── sitemap.xml                  # 自动生成的站点地图
├── index.html                   # 首页
├── about.html                   # 关于页面
├── contact.html                 # 联系页面
├── education.html               # 教育经历页面
├── projects.html                # 项目页面
├── skills.html                  # 技能页面
├── social.html                  # 社交页面
├── 404.html                     # 404 错误页面
├── _not-found.html              # 未找到页面
├── favicon.ico                  # 网站图标
├── _next/                       # Next.js 静态资源（必须）
│   ├── static/
│   │   ├── chunks/             # JavaScript 和 CSS 文件
│   │   └── media/              # 字体文件等
│   └── [构建ID]/
│       ├── _buildManifest.js
│       ├── _clientMiddlewareManifest.json
│       └── _ssgManifest.js
├── images/                      # 所有图片资源
│   ├── icons/
│   ├── location/
│   └── [其他图片文件]
├── assets/                      # 其他资源
│   └── resume.pdf
└── [其他 SVG 和静态文件]
```

## 🔍 文件说明

### 核心文件

- **`.nojekyll`**: 告诉 GitHub Pages 不要使用 Jekyll 处理，直接提供静态文件
- **`robots.txt`**: 搜索引擎爬虫配置，控制哪些页面可以被索引
- **`sitemap.xml`**: 站点地图，帮助搜索引擎发现和索引所有页面
- **`index.html`**: 网站首页

### 静态资源

- **`_next/`**: Next.js 构建生成的 JavaScript、CSS 和其他静态资源
- **`images/`**: 所有图片文件（头像、图标、地图等）
- **`assets/`**: PDF 等文档资源

## ✅ 上传前检查清单

### 必须检查项

- [ ] 所有 HTML 文件存在且可访问
- [ ] `_next/` 文件夹完整（包含所有 JS 和 CSS）
- [ ] 所有图片文件存在
- [ ] `robots.txt` 和 `sitemap.xml` 存在
- [ ] `.nojekyll` 文件存在（如果使用 GitHub Pages）

### SEO 检查项

- [ ] 更新 `src/app/layout.tsx` 中的 `metadataBase` 为实际域名
- [ ] 更新 `public/robots.txt` 中的 Sitemap URL
- [ ] 检查所有页面的 meta 标签是否正确
- [ ] 验证 `sitemap.xml` 包含所有页面

### 功能检查项

- [ ] 所有链接正常工作
- [ ] 图片正确加载
- [ ] 表单功能正常（如果适用）
- [ ] 移动端显示正常
- [ ] 页面加载速度正常

## 🚀 部署步骤

### 使用 GitHub Actions（推荐）

1. 上传所有源代码文件到 GitHub 仓库
2. 进入仓库 Settings → Pages
3. Source 选择 "GitHub Actions"
4. 推送代码到 main/master 分支
5. GitHub Actions 会自动构建和部署

### 手动部署

1. 运行 `npm run build` 生成 `out` 文件夹
2. 将 `out` 文件夹中的所有内容上传到 GitHub 仓库
3. 进入仓库 Settings → Pages
4. Source 选择 "Deploy from a branch"
5. Branch 选择 main/master
6. Folder 选择 `/ (root)`

## 📝 注意事项

1. **不要上传以下文件/文件夹**：
   - `node_modules/`
   - `.next/`
   - `.git/`
   - `.env*`
   - `*.tsbuildinfo`

2. **如果使用自定义域名**：
   - 在仓库根目录创建 `CNAME` 文件
   - 内容为你的域名（如：`yourdomain.com`）

3. **更新域名信息**：
   - 部署前记得更新 `src/app/layout.tsx` 中的 `metadataBase`
   - 更新 `public/robots.txt` 中的 Sitemap URL

## 🔗 相关文档

- `DEPLOYMENT.md` - 详细部署指南
- `README.md` - 项目说明
- `.github/workflows/README.md` - GitHub Actions 使用说明

